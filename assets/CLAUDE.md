# Cloopy Development Environment

- 対話には常に日本語で応答してください。
- この環境では Devbox を使用してパッケージ管理を行います。
  - 必要なパッケージがあれば `devbox global add <package>` で追加してください。
  - プロジェクトで明示的に Devbox を使用している場合のみ、プロジェクトローカルの `devbox add` を使ってください。
